*1. PREVIO

{
	*1.1. ARCHIVO DE REGISTRO

	{
	*Abrimos /creamos archivo de registro (guarda los resultados obtenidos)
	log using "C:\Users\Usuario\Documents\...\name.smcl" // para archivo smcl
	log using "C:\Users\Usuario\Documents\...\name.log" // para archivo log (es más facil llevarlo a excel)
	*Suspendemos el grabado de registro (idealmente hacerlo cuando se esta en un periodo de prueba y error)
	log off
	*Suspendemos el grabado de registro (reanudamos cuando tenemos el analisis en limpio)
	log on
	*Cerramos el archivo de registro (una vez concluido el analisis )//
	log close
	}
	
	*1.2  CAMBIAMOS DIRECTORIO
	{
	* Nos permite almacenar y abrir archivos del una carpeta (do files, log, bases de datos)
	cd "C:\Users\Usuario\Documents\...\..."
	
	}
	
	*1.3 MEMORIA
	{
	set memory 50m // aumentamos la memoria del sistema a 50 megas
	set max memory 2048m // estabblecemos la memoria maxima
				
	}
	
	*1.4 FUNCIONES
	{
	*Expresiones Logicas Significado
	
	& // Y (AND)
	| // O (OR)
	> //  Mayor que
	< //Menor que
	==  // Igual a
	! = // Diferente a
	>= // Mayor o Igual
	<= // Menor o Igual
	
*	Expresiones Aritmeticas

	+ // Mas, 
	- // Menos
	* // multiplicacion
	/ // division

	* Algunas funciones Matematicas
	abs() Valor absoluto
	comb(n,k) //the combinatorial function n!=/{k!(n - k)!}
	exp(x) /// función exponencial
	expm1(x) /// funcion e^x - 1
	round(x; y) // /redondea x en unidades de y;
	round(.,1)  /// rodondea a la integral m´as cercana.
	ln() /// logaritmo natural
	min(x1; x2; : : :) /// el m´inimo de x1; : : : ; xn
	max(x1; x2; : : :) /// el m´aximo de x1; : : : ; xn
	sqrt() /// raiz cuadrada
	sum() /// La suma para la expresi´on entre par´entesis.
	*uniform(100) /// Genera n´umeros aleatoreos entre 0 y 1 con una distribuci´on uniforme.
	help *functions* /// Ayuda respecto a una funcion
	* Crear variables con districiones estadisticas
	gen xy = rnormal(1000, 200) //normal con media 1000 y sd 200
	}


}
